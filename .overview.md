# BugFix 总结：地理维度分析模块柱状图数据标签修复

## 完成内容
修复了 keyword-classifier 项目（`/Users/zhangcage/WorkBuddy/2026-05-20-task-6/keyword-classifier/`）中地理维度分析模块城市TOP50柱状图数据标签不显示的问题。

## 根因
`js/ui.js` 第332行的 `dataLabelsPlugin` 中有一个跳过条件：
```javascript
if (chart.canvas && chart.canvas.id === 'chart-geo-city') return;
```
该条件强制跳过了城市TOP50柱状图的数据标签渲染。

## 修复
删除了上述跳过条件（2行代码），使所有柱状图（包括 `chart-geo-city`）的数据标签正常显示。

## 推送状态
- Commit: `e794eb4` (本地已提交)
- Push: 需要 GitHub 认证（暂未推送）
